// Simulated QR scanner logic (placeholder for real SDK)
window.addEventListener('DOMContentLoaded', () => {
  const scanButton = document.getElementById('scan-qr');
  scanButton?.addEventListener('click', () => {
    alert('🔍 QR code scanned! Payment confirmed.');
    // You could trigger a callback or redirect here
  });
});